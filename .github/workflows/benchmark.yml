name: Benchmark

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    timeout-minutes: 60

    steps:

      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
          repository: ${{ github.repository }}

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          run_install: true

      # For jshrink
      - name: Setup Composer
        uses: php-actions/composer@v6
        with:
          working_dir: packages/minifiers

      # For Google Closure Compiler
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Configure Git
        run: |
          git config user.name "minification-benchmarks automation"
          git config user.email actions@users.noreply.github.com

      - name: Benchmark
        run: pnpm bench-all

      - name: Commit benchmark results
        run: |
          # Ignored by Renovate
          git add packages/data/data/data.json
          if ! git diff --cached --quiet; then
            # [skip ci] so the generated commits don't trigger approval-required runs
            git commit -nm "chore: update benchmark results [skip ci]"
            git push
          fi

      - name: Update README
        run: pnpm update-readme
        env:
          VERCEL_AI_GATEWAY_API_KEY: ${{ secrets.VERCEL_AI_GATEWAY_API_KEY }}

      - name: Commit benchmark report
        run: |
          git add README.md
          if ! git diff --cached --quiet; then
            git commit -nm "docs: update benchmark report [skip ci]"
            git push
          fi
